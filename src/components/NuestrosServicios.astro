<section class="services" aria-labelledby="services-title">
	<div class="container">
		<div class="services-header">
			<div class="badge">
				<span class="badge-text">Tratamientos Especializados</span>
			</div>
			<h2 id="services-title">Transformamos tu sonrisa con tecnología avanzada</h2>
			<p class="intro">
				Tratamientos de odontología estética con materiales de alta calidad 
				y un enfoque 100% personalizado para resultados naturales y duraderos.
			</p>
		</div>

		<div class="services-grid">
			<div class="service-item featured">
				<div class="service-icon">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
						<line x1="8" y1="21" x2="16" y2="21"/>
						<line x1="12" y1="17" x2="12" y2="21"/>
					</svg>
				</div>
				<h3>Diseño Digital de Sonrisa</h3>
				<p>Visualiza tu nueva sonrisa antes del tratamiento con tecnología 3D avanzada.</p>
				<div class="service-highlight">Planificación precisa</div>
			</div>

			<div class="service-item">
				<div class="service-icon">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M12 2a10 10 0 1 0 10 10 4 4 0 0 1-5-5 4 4 0 0 1-5-5"/>
						<path d="M8.5 8.5v.01"/>
						<path d="M16 15.5v.01"/>
						<path d="M12 12v.01"/>
					</svg>
				</div>
				<h3>Carillas Cerámicas</h3>
				<p>Resultados duraderos y naturales con la más alta resistencia y estética.</p>
			</div>

			<div class="service-item">
				<div class="service-icon">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<polygon points="12,2 15.09,8.26 22,9.27 17,14.14 18.18,21.02 12,17.77 5.82,21.02 7,14.14 2,9.27 8.91,8.26"/>
					</svg>
				</div>
				<h3>Lentes de Disilicato</h3>
				<p>Máxima naturalidad y resistencia para una sonrisa perfecta y duradera.</p>
			</div>

			<div class="service-item">
				<div class="service-icon">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M9 11H5a2 2 0 0 0-2 2v3c0 1.1.9 2 2 2h4m6-6h4a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2h-4m-6 0a2 2 0 0 0-2-2v-3a2 2 0 0 0 2-2m6 0a2 2 0 0 1 2-2v-3a2 2 0 0 1-2-2"/>
					</svg>
				</div>
				<h3>Resinas Estéticas</h3>
				<p>Solución versátil para mejorar forma y color con resultados inmediatos.</p>
			</div>

			<div class="service-item">
				<div class="service-icon">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<circle cx="12" cy="12" r="3"/>
						<path d="M12 1v6m0 6v6m11-7h-6m-6 0H1"/>
					</svg>
				</div>
				<h3>Blanqueamiento Dental</h3>
				<p>Sonrisa más brillante y juvenil con técnicas seguras y efectivas.</p>
			</div>

			<div class="service-item">
				<div class="service-icon">
					<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/>
						<rect x="8" y="2" width="8" height="4" rx="1" ry="1"/>
						<path d="M9 14l2 2 4-4"/>
					</svg>
				</div>
				<h3>Tratamientos Personalizados</h3>
				<p>Cada sonrisa es única. Procedimientos 100% adaptados a tus necesidades.</p>
			</div>
		</div>
	</div>
</section>

<style>
	.services {
		position: relative;
		background: linear-gradient(180deg, #1a1a1a 0%, #0f0f0f 100%);
		padding: clamp(80px, 12vw, 140px) 0;
		overflow: hidden;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 clamp(20px, 4vw, 40px);
	}

	/* Header Section */
	.services-header {
		text-align: center;
		max-width: 700px;
		margin: 0 auto 60px;
	}

	.badge {
		display: inline-flex;
		align-items: center;
		background: linear-gradient(135deg, #f3c98b 0%, #e6b366 100%);
		color: #2c1810;
		padding: 8px 20px;
		border-radius: 50px;
		font-size: 14px;
		font-weight: 600;
		margin-bottom: 24px;
		box-shadow: 0 4px 12px rgba(243, 201, 139, 0.3);
	}

	.badge-text::before {
		content: '•';
		margin-right: 8px;
		font-size: 16px;
		font-weight: bold;
	}

	h2 {
		font-size: clamp(36px, 5.5vw, 52px);
		font-weight: 700;
		line-height: 1.2;
		margin: 0 0 24px;
		color: #ffffff;
		letter-spacing: -0.02em;
	}

	.intro {
		font-size: clamp(18px, 2.2vw, 20px);
		line-height: 1.6;
		color: rgba(255, 255, 255, 0.8);
		margin: 0;
		font-weight: 400;
	}

	/* Services Grid */
	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: 24px;
		margin-top: 60px;
	}

	.service-item {
		position: relative;
		background: rgba(255, 255, 255, 0.05);
		border-radius: 20px;
		padding: 32px 24px;
		box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
		border: 1px solid rgba(255, 255, 255, 0.1);
		transition: all 0.4s ease;
		text-align: left;
		overflow: hidden;
		backdrop-filter: blur(10px);
	}

	.service-item::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, #f3c98b 0%, #e6b366 100%);
		transform: scaleX(0);
		transition: transform 0.4s ease;
	}

	.service-item:hover {
		transform: translateY(-8px);
		box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
		background: rgba(255, 255, 255, 0.08);
		border-color: rgba(243, 201, 139, 0.3);
	}

	.service-item:hover::before {
		transform: scaleX(1);
	}

	.service-item.featured {
		background: linear-gradient(135deg, #f3c98b 0%, #e6b366 100%);
		color: #2c1810;
		transform: scale(1.02);
	}

	.service-item.featured::before {
		display: none;
	}

	.service-item.featured:hover {
		transform: scale(1.02) translateY(-8px);
	}

	/* Service Icons */
	.service-icon {
		width: 64px;
		height: 64px;
		background: linear-gradient(135deg, #f3c98b 0%, #e6b366 100%);
		border-radius: 16px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin-bottom: 20px;
		color: #2c1810;
		transition: all 0.3s ease;
	}

	.service-item.featured .service-icon {
		background: rgba(255, 255, 255, 0.2);
		color: #2c1810;
	}

	.service-item:hover .service-icon {
		transform: scale(1.1);
	}

	/* Service Content */
	.service-item h3 {
		font-size: 22px;
		font-weight: 600;
		margin: 0 0 12px;
		color: #ffffff;
		line-height: 1.3;
	}

	.service-item.featured h3 {
		color: #2c1810;
	}

	.service-item p {
		font-size: 16px;
		line-height: 1.6;
		color: rgba(255, 255, 255, 0.7);
		margin: 0 0 16px;
	}

	.service-item.featured p {
		color: rgba(44, 24, 16, 0.8);
	}

	/* Service Highlight */
	.service-highlight {
		display: inline-flex;
		align-items: center;
		background: rgba(255, 255, 255, 0.3);
		color: #2c1810;
		padding: 6px 12px;
		border-radius: 20px;
		font-size: 13px;
		font-weight: 600;
		margin-top: 8px;
	}

	.service-highlight::before {
		content: '★';
		margin-right: 6px;
		font-size: 14px;
	}

	/* Responsive Design */
	@media (max-width: 1024px) {
		.services-grid {
			grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
			gap: 20px;
		}
	}

	@media (max-width: 768px) {
		.services {
			padding: clamp(60px, 10vw, 100px) 0;
		}

		.services-header {
			margin-bottom: 40px;
		}

		.services-grid {
			grid-template-columns: 1fr;
			gap: 16px;
			margin-top: 40px;
		}

		.service-item {
			padding: 24px 20px;
		}

		.service-icon {
			width: 56px;
			height: 56px;
		}

		.service-item.featured {
			transform: none;
		}

		.service-item.featured:hover {
			transform: translateY(-8px);
		}
	}

	/* Animation */
	@media (prefers-reduced-motion: no-preference) {
		.service-item {
			animation: fade-in-up 0.6s ease forwards;
			opacity: 0;
			transform: translateY(30px);
		}

		.service-item:nth-child(1) { animation-delay: 0.1s; }
		.service-item:nth-child(2) { animation-delay: 0.2s; }
		.service-item:nth-child(3) { animation-delay: 0.3s; }
		.service-item:nth-child(4) { animation-delay: 0.4s; }
		.service-item:nth-child(5) { animation-delay: 0.5s; }
		.service-item:nth-child(6) { animation-delay: 0.6s; }

		@keyframes fade-in-up {
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	}
</style>