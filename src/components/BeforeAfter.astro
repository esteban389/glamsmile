<section class="before-after" aria-labelledby="before-after-title">
	<div class="container">
		<h2 id="before-after-title">Sonrisas reales. Resultados reales.</h2>
		<p class="subtitle">Transformaciones en solo 24/48 horas.</p>
		
		<div class="comparisons">
			<div class="comparison">
				<div class="comparison-container">
					<div class="before-section">
						<picture>
							<source srcset="/assets/before1.avif" type="image/avif" />
							<source srcset="/assets/before1.webp" type="image/webp" />
							<img src="/assets/before1.png" alt="Antes del tratamiento dental" loading="lazy" decoding="async" />
						</picture>
						<span class="label">Antes</span>
					</div>
					<div class="after-section">
						<picture>
							<source srcset="/assets/after1.avif" type="image/avif" />
							<source srcset="/assets/after1.webp" type="image/webp" />
							<img src="/assets/after1.png" alt="Después del tratamiento dental" loading="lazy" decoding="async" />
						</picture>
						<span class="label">Después</span>
					</div>
					<div class="slider-line"></div>
					<input type="range" class="slider" min="0" max="100" value="50" aria-label="Comparar antes y después" />
				</div>
			</div>
			
			<div class="comparison">
				<div class="comparison-container">
					<div class="before-section">
						<picture>
							<source srcset="/assets/before2.avif" type="image/avif" />
							<source srcset="/assets/before2.webp" type="image/webp" />
							<img src="/assets/before2.png" alt="Antes del tratamiento dental" loading="lazy" decoding="async" />
						</picture>
						<span class="label">Antes</span>
					</div>
					<div class="after-section">
						<picture>
							<source srcset="/assets/after2.avif" type="image/avif" />
							<source srcset="/assets/after2.webp" type="image/webp" />
							<img src="/assets/after2.png" alt="Después del tratamiento dental" loading="lazy" decoding="async" />
						</picture>
						<span class="label">Después</span>
					</div>
					<div class="slider-line"></div>
					<input type="range" class="slider" min="0" max="100" value="50" aria-label="Comparar antes y después" />
				</div>
			</div>
			
			<div class="comparison">
				<div class="comparison-container">
					<div class="before-section">
						<picture>
							<source srcset="/assets/before3.avif" type="image/avif" />
							<source srcset="/assets/before3.webp" type="image/webp" />
							<img src="/assets/before3.png" alt="Antes del tratamiento dental" loading="lazy" decoding="async" />
						</picture>
						<span class="label">Antes</span>
					</div>
					<div class="after-section">
						<picture>
							<source srcset="/assets/after3.avif" type="image/avif" />
							<source srcset="/assets/after3.webp" type="image/webp" />
							<img src="/assets/after3.png" alt="Después del tratamiento dental" loading="lazy" decoding="async" />
						</picture>
						<span class="label">Después</span>
					</div>
					<div class="slider-line"></div>
					<input type="range" class="slider" min="0" max="100" value="50" aria-label="Comparar antes y después" />
				</div>
			</div>
		</div>
		
		<div class="gallery">
			<h3>Más transformaciones</h3>
			<div class="gallery-grid">
				<div class="gallery-item">
					<picture>
						<source srcset="/assets/before_after.avif" type="image/avif" />
						<source srcset="/assets/before_after.webp" type="image/webp" />
						<img src="/assets/before_after.jpg" alt="Transformación dental antes y después" loading="lazy" decoding="async" />
					</picture>
				</div>
				<div class="gallery-item">
					<picture>
						<source srcset="/assets/before_after2.avif" type="image/avif" />
						<source srcset="/assets/before_after2.webp" type="image/webp" />
						<img src="/assets/before_after2.jpg" alt="Transformación dental antes y después" loading="lazy" decoding="async" />
					</picture>
				</div>
				<div class="gallery-item">
					<picture>
						<source srcset="/assets/before_after3.avif" type="image/avif" />
						<source srcset="/assets/before_after3.webp" type="image/webp" />
						<img src="/assets/before_after3.jpg" alt="Transformación dental antes y después" loading="lazy" decoding="async" />
					</picture>
				</div>
				<div class="gallery-item">
					<picture>
						<source srcset="/assets/before_after4.avif" type="image/avif" />
						<source srcset="/assets/before_after4.webp" type="image/webp" />
						<img src="/assets/before_after4.jpg" alt="Transformación dental antes y después" loading="lazy" decoding="async" />
					</picture>
				</div>
			</div>
		</div>
		
		<div class="footer-note">
			<p>Cada sonrisa cuenta una historia.</p>
		</div>
		
		<div class="counter">
			<span>+5000 sonrisas transformadas</span>
		</div>
	</div>
</section>

<style>
	.before-after {
		background: var(--why-surface);
		color: var(--why-text);
		padding: clamp(48px, 8vw, 120px) 0;
		position: relative;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 clamp(16px, 3vw, 24px);
		text-align: center;
	}

	.before-after h2 {
		margin: 0 0 8px;
		font-size: clamp(28px, 4vw, 42px);
		font-weight: var(--font-weight-semibold);
		color: var(--why-text);
	}

	.subtitle {
		margin: 0 0 48px;
		color: var(--why-muted);
		font-size: clamp(16px, 1.8vw, 18px);
		line-height: 1.5;
	}

	.comparisons {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 32px;
		margin-bottom: 48px;
	}

	.comparison {
		position: relative;
		border-radius: 16px;
		overflow: hidden;
		box-shadow: 0 12px 30px rgba(0,0,0,0.08);
		background: var(--why-surface);
	}

	.comparison-container {
		position: relative;
		width: 100%;
		height: 640px;
		overflow: hidden;
	}

	.before-section,
	.after-section {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		overflow: hidden;
	}

	.after-section {
		clip-path: inset(0 50% 0 0);
	}

	.comparison img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.after-section img {
		filter: brightness(1.1) contrast(1.05);
		box-shadow: 0 0 20px rgba(243,201,139,0.3);
	}

	.label {
		position: absolute;
		bottom: 12px;
		background: rgba(0,0,0,0.7);
		color: white;
		padding: 4px 12px;
		border-radius: 12px;
		font-size: 12px;
		font-weight: 500;
		backdrop-filter: blur(4px);
	}

	.before-section .label {
		left: 12px;
	}

	.after-section .label {
		right: 12px;
	}

	.slider-line {
		position: absolute;
		top: 0;
		left: 50%;
		width: 2px;
		height: 100%;
		background: var(--why-accent);
		transform: translateX(-50%);
		z-index: 2;
		box-shadow: 0 0 10px rgba(243,201,139,0.5);
	}

	.slider {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: transparent;
		outline: none;
		cursor: ew-resize;
		z-index: 3;
		-webkit-appearance: none;
		appearance: none;
	}

	.slider::-webkit-slider-thumb {
		-webkit-appearance: none;
		appearance: none;
		width: 20px;
		height: 20px;
		border-radius: 50%;
		background: var(--why-accent);
		cursor: ew-resize;
		border: 2px solid white;
		box-shadow: 0 2px 8px rgba(0,0,0,0.2);
	}

	.slider::-moz-range-thumb {
		width: 20px;
		height: 20px;
		border-radius: 50%;
		background: var(--why-accent);
		cursor: ew-resize;
		border: 2px solid white;
		box-shadow: 0 2px 8px rgba(0,0,0,0.2);
	}

	.footer-note {
		margin-bottom: 16px;
	}

	.footer-note p {
		margin: 0;
		color: var(--why-muted);
		font-style: italic;
		font-size: clamp(14px, 1.6vw, 16px);
	}

	.counter {
		background: color-mix(in oklab, var(--why-accent) 8%, var(--why-surface) 92%);
		border: 1px solid color-mix(in oklab, var(--why-accent) 20%, transparent);
		border-radius: 20px;
		padding: 8px 16px;
		display: inline-block;
	}

	.counter span {
		font-size: clamp(12px, 1.4vw, 14px);
		font-weight: 500;
		color: var(--why-text);
	}

	.gallery {
		margin: 48px 0;
	}

	.gallery h3 {
		text-align: center;
		margin: 0 0 24px;
		font-size: clamp(20px, 2.5vw, 24px);
		font-weight: var(--font-weight-semibold);
		color: var(--why-text);
	}

	.gallery-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 20px;
	}

	.gallery-item {
		border-radius: 12px;
		overflow: hidden;
		box-shadow: 0 8px 20px rgba(0,0,0,0.08);
		background: var(--why-surface);
		transition: transform 200ms ease, box-shadow 200ms ease;
	}

	.gallery-item:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 30px rgba(0,0,0,0.12);
	}

	.gallery-item img {
		width: 100%;
		height: auto;
		display: block;
	}

	@media (max-width: 768px) {
		.comparisons {
			grid-template-columns: 1fr;
			gap: 24px;
		}
		
		.comparison-container {
			height: 480px;
		}
		
		.gallery-grid {
			grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
			gap: 16px;
		}
	}
</style>

<script>
	document.addEventListener('DOMContentLoaded', function() {
		const sliders = document.querySelectorAll('.slider');
		
		sliders.forEach(slider => {
			const container = slider.closest('.comparison-container');
			const afterSection = container?.querySelector('.after-section');
			const sliderLine = container?.querySelector('.slider-line');
			
			function updateSlider() {
				const value = slider.value;
				afterSection.style.clipPath = `inset(0 ${100 - value}% 0 0)`;
				sliderLine.style.left = `${value}%`;
			}
			
			slider.addEventListener('input', updateSlider);
			
			// Auto-animate on scroll
			const observer = new IntersectionObserver((entries) => {
				entries.forEach(entry => {
					if (entry.isIntersecting) {
						slider.value = 50;
						updateSlider();
					}
				});
			}, { threshold: 0.5 });
			
			observer.observe(container);
		});
	});
</script>
