<section class="location-contact" aria-labelledby="contact-title">
	<div class="container">
		<div class="content-section">
			<h2 id="contact-title">Estamos aquí para ti</h2>
			<p class="intro">
				Visítanos en nuestra clínica o agenda tu cita desde cualquier parte del mundo.
				Nuestro equipo está listo para acompañarte en el proceso de lograr la sonrisa que siempre soñaste.
			</p>

			<div class="contact-info">
				<div class="contact-item">
					<span class="icon" aria-hidden="true">📍</span>
					<div>
						<h3>Dirección</h3>
						<p>Calle 123 #45-67, Bogotá, Colombia</p>
					</div>
				</div>

				<div class="contact-item">
					<span class="icon" aria-hidden="true">📞</span>
					<div>
						<h3>Teléfono</h3>
						<p>+57 300 000 0000</p>
					</div>
				</div>

				<div class="contact-item">
					<span class="icon" aria-hidden="true">📧</span>
					<div>
						<h3>Correo</h3>
						<p>contacto@glamsmile.com</p>
					</div>
				</div>

				<div class="contact-item">
					<span class="icon" aria-hidden="true">📲</span>
					<div>
						<h3>Instagram</h3>
						<p>@glamsmile</p>
					</div>
				</div>
			</div>
		</div>

		<div class="map-section">
			<div class="map-container">
				<iframe
					src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3976.6439328998287!2d-74.072092!3d4.711084!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8e3f9a2e5d4c5d5d%3A0x1234567890abcdef!2sCalle%20123%20%2345-67%2C%20Bogot%C3%A1%2C%20Colombia!5e0!3m2!1ses!2sco!4v1234567890123!5m2!1ses!2sco"
					width="100%"
					height="100%"
					style="border:0;"
					allowfullscreen=""
					loading="lazy"
					referrerpolicy="no-referrer-when-downgrade"
					aria-label="Ubicación de GlamSmile en Bogotá"
				></iframe>
			</div>
		</div>
	</div>
</section>

<style>
	.location-contact {
		position: relative;
		background: var(--why-bg);
		color: var(--why-text);
		overflow: clip;
		padding: clamp(48px, 8vw, 120px) 0;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: clamp(30px, 4vw, 60px);
		align-items: center;
		padding: 0 clamp(16px, 3vw, 24px);
		position: relative;
	}

	.content-section {
		display: flex;
		flex-direction: column;
		gap: 24px;
	}

	h2 {
		margin: 0;
		font-size: clamp(28px, 4vw, 42px);
		color: var(--why-text);
		font-weight: 700;
	}

	h3 {
		margin: 0;
		font-size: clamp(20px, 2.5vw, 24px);
		color: var(--why-text);
		font-weight: 600;
	}

	.intro {
		font-size: clamp(16px, 1.8vw, 18px);
		line-height: 1.6;
		color: var(--why-muted);
		margin: 0;
	}

	.contact-info {
		display: flex;
		flex-direction: column;
		gap: 16px;
	}

	.contact-item {
		display: flex;
		align-items: center;
		gap: 16px;
		background: var(--why-surface);
		border: 1px solid color-mix(in oklab, #000 8%, transparent);
		border-radius: 12px;
		padding: 16px 20px;
		transition: all 0.3s ease;
	}

	.contact-item:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 25px rgba(0,0,0,0.1);
		border-color: var(--why-accent);
	}

	.contact-item .icon {
		font-size: 24px;
		width: 40px;
		height: 40px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: color-mix(in oklab, var(--why-accent) 15%, transparent);
		border-radius: 10px;
	}

	.contact-item div h3 {
		font-size: 16px;
		margin-bottom: 4px;
	}

	.contact-item div p {
		margin: 0;
		font-size: 14px;
		color: var(--why-muted);
	}

	.map-section {
		position: relative;
	}

	.map-container {
		background: var(--why-surface);
		border-radius: 16px;
		box-shadow: 0 20px 50px rgba(0,0,0,0.12);
		overflow: hidden;
		height: 500px;
	}

	.map-container iframe {
		width: 100%;
		height: 100%;
		display: block;
	}

	@media (max-width: 768px) {
		.container {
			grid-template-columns: 1fr;
			gap: 32px;
		}

		.map-container {
			height: 300px;
		}

		h2 {
			font-size: 28px;
		}
	}
</style>