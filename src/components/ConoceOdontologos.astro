<section class="team" aria-labelledby="team-title">
	<div class="container">
		<div class="team-header">
			<h2 id="team-title">Conoce a nuestros especialistas</h2>
			<p class="intro">
				Detrás de cada sonrisa transformada hay un equipo de especialistas en Odontología Estética con años de experiencia, formación internacional y pasión por lo que hacen.
			</p>
		</div>

		<div class="team-grid">
			<article class="doctor-card">
				<div class="doctor-photo">
					<img src="/assets/girl.jpg" alt="Dra. Sofia Ramirez" loading="lazy" width="300" height="400" />
				</div>
				<div class="doctor-info">
					<h3 class="doctor-name">Dra. Sofía Ramírez</h3>
					<p class="doctor-specialty">Odontología Estética</p>
					<p class="doctor-bio">
						"Más de 10 años de experiencia ayudando a pacientes a recuperar su confianza a través de sonrisas naturales y armónicas."
					</p>
				</div>
			</article>

			<article class="doctor-card">
				<div class="doctor-photo">
					<img src="/assets/guy.jpg" alt="Dr. Carlos Mendez" loading="lazy" width="300" height="400" />
				</div>
				<div class="doctor-info">
					<h3 class="doctor-name">Dr. Carlos Méndez</h3>
					<p class="doctor-specialty">Carillas Cerámicas</p>
					<p class="doctor-bio">
						"Especialista en rehabilitación estética con más de 12 años de experiencia en carillas cerámicas y lentes dentales."
					</p>
					</div>
			</article>

			<article class="doctor-card">
				<div class="doctor-photo">
					<img src="/assets/girl.jpg" alt="Dra. Ana Lucia Torres" loading="lazy" width="300" height="400" />
				</div>
				<div class="doctor-info">
					<h3 class="doctor-name">Dra. Ana Lucía Torres</h3>
					<p class="doctor-specialty">Diseño Digital de Sonrisa</p>
					<p class="doctor-bio">
						"Pionera en tecnología digital aplicada a la odontología estética, especializada en planificación 3D y diseño predictivo."
					</p>
				</div>
			</article>
		</div>
	</div>
</section>

<style>
	.team {
		padding: clamp(60px, 8vw, 100px) 20px;
		background: var(--why-bg);
		position: relative;
	}

	.team-header {
		text-align: center;
		max-width: 800px;
		margin: 0 auto 60px;
	}

	.team-header h2 {
		font-size: clamp(32px, 4vw, 48px);
		font-weight: var(--font-weight-bold);
		margin: 0 0 20px;
		color: var(--why-text);
	}

	.team-header .intro {
		font-size: clamp(16px, 1.5vw, 18px);
		line-height: 1.6;
		color: var(--why-muted);
		margin: 0;
	}

	.team-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 40px;
		max-width: 1200px;
		margin: 0 auto;
	}

	.doctor-card {
		background: var(--why-surface);
		border-radius: 16px;
		overflow: hidden;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
		transition: all 0.3s ease;
		border: 1px solid rgba(0, 0, 0, 0.05);
	}

	.doctor-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
	}

	.doctor-photo {
		width: 100%;
		height: 300px;
		overflow: hidden;
		position: relative;
	}

	.doctor-photo img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: transform 0.3s ease;
	}

	.doctor-card:hover .doctor-photo img {
		transform: scale(1.05);
	}

	.doctor-info {
		padding: 30px;
	}

	.doctor-name {
		font-size: 24px;
		font-weight: var(--font-weight-bold);
		margin: 0 0 8px;
		color: var(--why-text);
	}

	.doctor-specialty {
		font-size: 16px;
		font-weight: var(--font-weight-medium);
		color: var(--why-accent);
		margin: 0 0 15px;
	}

	.doctor-bio {
		font-size: 15px;
		line-height: 1.6;
		color: var(--why-muted);
		margin: 0 0 20px;
		font-style: italic;
	}

	.doctor-social {
		display: flex;
		gap: 15px;
	}

	.social-link {
		width: 40px;
		height: 40px;
		border-radius: 50%;
		background: rgba(243, 201, 139, 0.1);
		color: var(--why-accent);
		display: flex;
		align-items: center;
		justify-content: center;
		text-decoration: none;
		transition: all 0.3s ease;
		border: 1px solid rgba(243, 201, 139, 0.2);
	}

	.social-link:hover {
		background: var(--why-accent);
		color: var(--why-surface);
		transform: translateY(-2px);
	}

	.social-link:focus {
		outline: 2px solid var(--color-focus);
		outline-offset: 2px;
	}

	/* Responsive adjustments */
	@media (max-width: 768px) {
		.team {
			padding: 50px 20px;
		}

		.team-header {
			margin-bottom: 40px;
		}

		.team-grid {
			grid-template-columns: 1fr;
			gap: 30px;
		}

		.doctor-photo {
			height: 250px;
		}

		.doctor-info {
			padding: 25px;
		}

		.doctor-name {
			font-size: 22px;
		}

		.doctor-specialty {
			font-size: 15px;
		}

		.doctor-bio {
			font-size: 14px;
		}
	}

	@media (max-width: 480px) {
		.doctor-photo {
			height: 220px;
		}

		.doctor-info {
			padding: 20px;
		}

		.doctor-name {
			font-size: 20px;
		}

		.doctor-specialty {
			font-size: 14px;
		}

		.doctor-bio {
			font-size: 13px;
			margin-bottom: 15px;
		}

		.social-link {
			width: 35px;
			height: 35px;
		}
	}

	/* Performance optimization */
	@media (prefers-reduced-motion: reduce) {
		.doctor-card {
			transition: none;
		}

		.doctor-photo img {
			transition: none;
		}

		.social-link {
			transition: none;
		}
	}
</style>