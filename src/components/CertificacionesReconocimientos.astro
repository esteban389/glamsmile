<section class="certificaciones" aria-labelledby="certificaciones-title">
	<div class="container">
		<header class="intro">
			<h2 id="certificaciones-title">Certificaciones y Reconocimientos</h2>
			<p>
				Nuestra experiencia está respaldada por certificaciones internacionales, reconocimientos en congresos de Odontología Estética y formación en las técnicas más avanzadas a nivel global.
			</p>
		</header>

		<div class="certifications-grid">
			<div class="certification-card">
				<div class="logo-placeholder" aria-hidden="true">🏆</div>
				<h3>Certificación en Carillas Cerámicas</h3>
				<p class="location">Miami, USA</p>
			</div>

			<div class="certification-card">
				<div class="logo-placeholder" aria-hidden="true">👥</div>
				<h3>Miembros Activos Sociedad Colombiana de Odontología Estética</h3>
				<p class="location">Colombia</p>
			</div>

			<div class="certification-card">
				<div class="logo-placeholder" aria-hidden="true">🎯</div>
				<h3>Reconocimiento en Congreso Internacional de Estética Dental</h3>
				<p class="location">2024</p>
			</div>

			<div class="certification-card">
				<div class="logo-placeholder" aria-hidden="true">⚡</div>
				<h3>Entrenamiento en Técnicas Mínimamente Invasivas</h3>
				<p class="location">Madrid, España</p>
			</div>
		</div>
	</div>
</section>

<style>
	.certificaciones {
		position: relative;
		background: #f8f9fa;
		color: #2d3748;
		overflow: clip;
		padding: clamp(48px, 8vw, 120px) 0;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 clamp(16px, 3vw, 24px);
		position: relative;
	}

	.intro {
		text-align: center;
		max-width: 800px;
		margin: 0 auto clamp(48px, 6vw, 72px);
	}

	.intro h2 {
		margin: 0 0 20px;
		font-size: clamp(28px, 4vw, 42px);
		font-weight: 700;
		color: #1a202c;
	}

	.intro p {
		margin: 0;
		font-size: clamp(16px, 1.2vw, 18px);
		line-height: 1.7;
		color: #4a5568;
	}

	.certifications-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: clamp(24px, 3vw, 32px);
	}

	.certification-card {
		background: white;
		border: 1px solid #e2e8f0;
		border-radius: 16px;
		padding: 32px 24px;
		text-align: center;
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.certification-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(90deg, transparent, rgba(243,201,139,0.3), transparent);
	}

	.certification-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 12px 24px rgba(0,0,0,0.08);
		border-color: rgba(243,201,139,0.3);
	}

	.logo-placeholder {
		width: 80px;
		height: 80px;
		margin: 0 auto 20px;
		background: linear-gradient(135deg, #f3c98b 0%, #e5b574 100%);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 36px;
		color: white;
		box-shadow: 0 4px 12px rgba(243,201,139,0.3);
	}

	.certification-card h3 {
		margin: 0 0 12px;
		font-size: clamp(16px, 1.8vw, 20px);
		font-weight: 600;
		color: #1a202c;
		line-height: 1.4;
	}

	.location {
		margin: 0;
		font-size: 14px;
		color: #718096;
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	@media (max-width: 768px) {
		.certifications-grid {
			grid-template-columns: 1fr;
			gap: 20px;
		}

		.certification-card {
			padding: 24px 20px;
		}

		.logo-placeholder {
			width: 64px;
			height: 64px;
			font-size: 28px;
		}
	}
</style>